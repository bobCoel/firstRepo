source 'http://rubygems.org'

gem 'sinatra'
gem 'json'
gem 'rack'
gem 'rack-contrib'
gem 'rake'
gem 'rubysl-base64'
gem 'rookout'

# These are the dependencies that are used only for unit tests.
group :test do
  gem "rspec"
  gem "rack-test"
end
